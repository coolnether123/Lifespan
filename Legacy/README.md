This folder archives the stale pre-SDK duplicate Lifespan source tree and committed package artifacts.
It was moved out of the repo root because Lifespan.sln builds the active SDK project instead.
The active source tree is Lifespan/.
